<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/head') %>
    <title>Wave Runners</title>
    <link rel="stylesheet" href="styles/register.css">


</head>

<body>
    <header>
        <a href="/">
            <div class="headerflex">
                <img class="logotipo" src="/images/logos/circulo.jpg" alt="Logo de WaveRunners">
    
            </div>
        </a>
    </header>
    <main class="mainbody">
        <section class="sectionbody">
            <h3 class="crearcuenta">Crear cuenta</h3>

            <form action="/register" method="POST" enctype="multipart/form-data">
                <div class="divflex">
                    <div class="nombre">
                        <label for="name">Nombre completo</label>
                        <input type="text" name="name" id="nombre_completo" placeholder="Ingrese tu nombre completo"
                        value="<%= locals.oldData ? oldData.name : null %>">
                        <% if (locals.errors && errors.name) { %>
                            <div class="text-danger">
                                <%= errors.name.msg %> 
                            </div>
                        <% } %>
                    </div>
                    <div class="nombre">
                        <label for="username">Nombre de usuario</label>
                        <input type="text" name="username" id="nombre_usuario" placeholder="Ingrese un usuario"
                        value="<%= locals.oldData ? oldData.username : null %>">
                        <% if (locals.errors && errors.username) { %>
                            <div class="text-danger">
                                <%= errors.username.msg %> 
                            </div>
                        <% } %>
                    </div>
                    <div class="mail">
                        <label for="email">Correo electrónico</label>
                        <input type="email" name="email"  id="mail" placeholder="Ingrese un email"
                        value ="<%= locals.oldData ? oldData.email : null %>">
                        <% if (locals.errors && errors.email) { %>
                            <div class="text-danger">
                                <%= errors.email.msg %>
                            </div>
                        <% } %>
                    </div>
                    <div class="contraseña">
                        <label for="password">Contraseña</label>
                        <input type="password" name="password"  id="contraseña" placeholder="Ingrese una contraseña">
                            <% if (locals.errors && errors.password) { %>
                                <div class="text-danger">
                                    <%= errors.password.msg %>
                                </div>
                            <% } %>
                    </div>
                    <div class="contraseña2">
                        <label for="pass_confirm">Confirmá tu contraseña</label>
                        <input type="password" name="pass_confirm" id="confirmar-contraseña" placeholder="Confirma tu contraseña">
                    </div>
                    <div>
                        <label for="avatar">Dejanos una foto canchera</label>
                        <input type="file" name="avatar" id="avatar">
                    </div>
                    <div class="divboton">
                        <button class="boton" type="submit">Crea tu cuenta de Wave Runners</button>
                    </div>

                </div>
            </form>
            <div class="parrafo">
                <p>Al crear una cuenta, aceptas las <a class="hipervinculo" href="#">Condiciones de Uso</a> y el <a
                        class="hipervinculo" href="#">Aviso de Privacidad</a> de Wave Runners</p>
            </div>
            <div class="fondo">
                <p>
                    ¿Ya tenes cuenta? <a class="hipervinculo" href="/login">Iniciar sesión</a>
                </p>
            </div>

        </section>




    </main>
    <%- include('../partials/footer') %>
</body>

</html>